document.addEventListener("DOMContentLoaded", function() {
    // Assume influencer's first name is stored in a variable firstName
    var firstName = "John"; // Replace with actual first name retrieved from sign-in
    
    // Display influencer's first name in greeting
    document.getElementById("firstName").innerText = firstName;
  });
  